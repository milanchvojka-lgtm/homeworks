// Domácí Úkoly — Prisma schema
// Schéma se rozšiřuje milestone po milestone podle IMPLEMENTATION_PLAN.md.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
